# 快速启动指南

## 第一步：准备工作（5 分钟）

### 1. 安装微信开发者工具
下载地址：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

### 2. 注册小程序账号
访问：https://mp.weixin.qq.com/
- 点击"立即注册"
- 选择"小程序"
- 填写邮箱、密码等信息
- 完成邮箱激活

### 3. 获取 AppID
- 登录小程序后台
- 进入"开发" -> "开发管理"
- 复制您的 AppID（类似：wx0000000000000000）

## 第二步：导入项目（2 分钟）

1. 打开微信开发者工具
2. 选择"导入项目"
3. 项目目录：选择 `makeupApp` 文件夹
4. AppID：填入您刚才复制的 AppID
5. 点击"导入"

## 第三步：配置云开发（10 分钟）

### 1. 开通云开发
1. 在开发者工具顶部点击"云开发"按钮
2. 首次使用会提示开通，点击"开通"
3. 选择"个人版"或"基础版"即可
4. 等待环境创建完成

### 2. 记录环境 ID
- 环境创建完成后，复制环境 ID（类似：cloud1-xxx）
- 后续配置需要用到

### 3. 修改配置文件

**修改 `app.js`**
```javascript
// 找到第 7 行，替换为您的云环境 ID
wx.cloud.init({
  env: 'cloud1-xxx',  // 改为您的环境 ID
  traceUser: true,
})
```

**修改 `project.config.json`**
```json
{
  "appid": "wx0000000000000000"  // 改为您的小程序 AppID
}
```

## 第四步：初始化数据库（5 分钟）

### 1. 上传云函数
1. 在开发者工具左侧文件树找到 `cloudfunctions/initDatabase`
2. 右键点击该文件夹
3. 选择"上传并部署：云端安装依赖"
4. 等待上传完成（约 1-2 分钟）

### 2. 执行初始化
1. 点击顶部"云开发"按钮，打开云开发控制台
2. 进入"云函数"页面
3. 找到 `initDatabase` 函数
4. 右键点击，选择"执行"
5. 查看日志，确认显示"数据库初始化成功"

### 3. 验证数据
1. 在云开发控制台进入"数据库"
2. 应该能看到以下集合：
   - categories（分类）
   - subcategories（子分类）
   - works（作品）
   - banners（轮播图）
   - bookings（预约）

## 第五步：准备图片资源（15 分钟）

### 1. TabBar 图标（必需）
在 `images/` 目录下创建以下图标：

**首页图标**
- `home.png` - 房子图标（未选中，灰色）
- `home-active.png` - 房子图标（选中，金色 #d4a574）

**精选图标**
- `star.png` - 星星图标（未选中，灰色）
- `star-active.png` - 星星图标（选中，金色 #d4a574）

**预约图标**
- `calendar.png` - 日历图标（未选中，灰色）
- `calendar-active.png` - 日历图标（选中，金色 #d4a574）

> 图标尺寸建议：81x81px，PNG 格式，背景透明

### 2. 分类图标（可选）
暂时可以使用默认颜色圆圈，后续可以替换为：
- `category-hanfu.png` - 汉服相关图片
- `category-qipao.png` - 旗袍相关图片
- 等等...

### 3. 快速解决方案
如果暂时没有图标，可以：
1. 使用在线图标库：https://www.iconfont.cn/
2. 使用 Figma 制作简单图标
3. 从 Flaticon 等网站下载免费图标

## 第六步：编译运行（1 分钟）

1. 点击开发者工具顶部的"编译"按钮
2. 在模拟器中查看小程序效果
3. 如果没有报错，恭喜您，小程序已经成功运行！

## 第七步：上传作品数据（10 分钟）

### 方法一：通过云开发控制台
1. 打开云开发控制台
2. 进入"数据库" -> "works"集合
3. 点击"添加数据"
4. 填写作品信息：
   ```javascript
   {
     "title": "汉服 | 示例作品",
     "subtitle": "古典系列",
     "categoryId": 1,
     "categoryName": "汉服",
     "subcategoryId": 2,
     "subcategoryName": "整体造型",
     "coverUrl": "https://...",  // 上传封面图后获取的 URL
     "images": ["https://..."],   // 图片 URL 数组
     "description": "作品描述",
     "details": ["细节 1", "细节 2"],
     "isFeatured": true,
     "enabled": true,
     "order": 1
   }
   ```
5. 点击"确定"保存

### 方法二：使用初始化数据
初始化云函数时已经创建了示例数据，可以直接使用。

## 第八步：真机预览（2 分钟）

1. 在开发者工具顶部点击"预览"
2. 使用微信扫描二维码
3. 在手机上查看小程序效果

## 第九步：上传审核（可选）

如果准备发布小程序：

### 1. 上传代码
1. 点击开发者工具顶部的"上传"按钮
2. 填写版本号和备注
3. 点击"上传"

### 2. 提交审核
1. 登录小程序后台：https://mp.weixin.qq.com/
2. 进入"版本管理"
3. 找到刚上传的版本，点击"提交审核"
4. 填写审核信息（服务类目、测试账号等）
5. 提交等待审核

### 3. 发布上线
审核通过后，点击"发布"即可上线

## 常见问题排查

### ❌ 编译报错"app.json 未找到"
**解决**：确认项目目录正确，`app.json` 文件存在于项目根目录

### ❌ 云函数调用失败
**解决**：
1. 检查云环境 ID 是否正确
2. 确认云函数已上传并部署
3. 查看云函数日志排查错误

### ❌ 图片无法显示
**解决**：
1. 检查图片 URL 是否正确
2. 确认图片已上传到云存储
3. 检查图片路径权限设置

### ❌ TabBar 图标不显示
**解决**：
1. 确认图标文件已放入 `images/` 目录
2. 检查图标文件名是否正确
3. 图标尺寸是否为 81x81px

### ❌ 数据库查询为空
**解决**：
1. 确认已执行初始化云函数
2. 检查数据库集合是否创建成功
3. 手动添加测试数据验证

## 下一步优化

完成基础配置后，您可以：

1. **自定义样式**
   - 修改主色调（在 app.wxss 中搜索 #d4a574 替换）
   - 调整布局和间距
   
2. **添加更多功能**
   - 用户登录功能
   - 评论和点赞
   - 在线客服
   
3. **性能优化**
   - 图片压缩
   - 数据缓存
   - 懒加载优化

4. **后台管理**
   - 开发管理后台
   - 批量上传作品
   - 预约管理

## 技术支持

- 微信小程序官方文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- 云开发文档：https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html

---

**预计总耗时：约 50 分钟**

按照以上步骤操作，您应该能成功运行这个造型师小程序。如有问题，请查看常见问题排查部分。
